import {UniqueIdValidationPipe} from './unique-id-validation.pipe';

describe('UniqueIdValidationPipe', () => {
  it('should be defined', () => {
    expect(new UniqueIdValidationPipe()).toBeDefined();
  });
});
